import { makeStyles } from '@mui/styles'

export const UseStyle = makeStyles(() => ({
  mainContainer: {
    marginLeft: 'auto',
    marginBottom: '1.5rem',
  },
  orderStatus: {
    fontSize: '27px',
    color: '#3C49FF',
    fontWeight: 'bold',
    marginLeft: '3.5rem',
  },
}))
